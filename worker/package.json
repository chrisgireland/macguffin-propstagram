{
  "name": "propstagram-api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute propstagram --file=migrations/0001_init.sql --local",
    "migrate:remote": "wrangler d1 execute propstagram --file=migrations/0001_init.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
